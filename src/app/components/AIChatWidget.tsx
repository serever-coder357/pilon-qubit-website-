'use client';

// This component is intentionally disabled to avoid duplicate chat widgets.
// The primary AI assistant is handled by the main GPT-4 chat component.
export default function AIChatWidget() {
  return null;
}
