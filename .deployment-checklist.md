# Deployment Checklist for PILON Qubit Website

## ⚠️ CRITICAL: Always Follow This Process

### Pre-Deployment Checklist

- [ ] **Verify you're in the correct directory**
  ```bash
  pwd  # Should show: /home/ubuntu/pilon-qubit-website-
  ```

- [ ] **Test changes on dev server FIRST**
  - Dev server: https://3002-ibvkptqgiaioidqfdtz1i-2253fba3.manusvm.computer/
  - Navigate to changed pages
  - Test all interactive elements
  - Check mobile responsiveness
  - Verify widgets are working

- [ ] **Check git status**
  ```bash
  git status
  git diff  # Review all changes
  ```

- [ ] **Commit with descriptive message**
  ```bash
  git add <files>
  git commit -m "Clear description of what changed"
  ```

- [ ] **Pull latest changes before pushing**
  ```bash
  git pull origin main
  # Resolve any conflicts if needed
  ```

- [ ] **Push to GitHub**
  ```bash
  git push origin main
  ```

- [ ] **Monitor Vercel deployment**
  - Check https://vercel.com/dashboard
  - Wait for build to complete (2-3 minutes)
  - Check deployment logs for errors

- [ ] **Verify production site**
  - Visit https://pilonqubitventures.com
  - Test the specific pages you changed
  - Hard refresh (Ctrl+Shift+R) to clear cache
  - Test on mobile device

---

## 🚨 Common Mistakes to Avoid

1. **DON'T edit files without testing locally first**
2. **DON'T push directly to production without dev server verification**
3. **DON'T assume changes work - always verify visually**
4. **DON'T skip the hard refresh - browser cache can fool you**

---

## 📁 Project Structure

**Correct directory:** `/home/ubuntu/pilon-qubit-website-` (with trailing dash)
**Dev server port:** 3002
**GitHub repo:** `github.com/serever-coder357/pilon-qubit-website-`
**Production URL:** `pilonqubitventures.com`
**Vercel domain:** `pilon-qubit-ventures.vercel.app`

---

## 🔧 Quick Commands

```bash
# Start dev server (if not running)
cd /home/ubuntu/pilon-qubit-website- && npm run dev

# Check what's running on port 3002
lsof -i :3002

# View recent commits
git log --oneline -5

# Undo last commit (if not pushed yet)
git reset --soft HEAD~1

# Force pull (discard local changes)
git fetch origin && git reset --hard origin/main
```

---

## 📝 Deployment Log Template

```
Date: YYYY-MM-DD
Changes: 
- [ ] Description of change 1
- [ ] Description of change 2

Dev server tested: ✅ / ❌
Production verified: ✅ / ❌
Issues found: None / [describe issues]
```
